#strings

name = input("Enter your full name:")
#result = len(name)# length
#result = name.find("h") # finds the first occurrence of the string
#it returns -1 if it cant find any string
#name = name.upper()# uppercase
#name = name.lower()#Lowercase
#name = name.isdigit() # returns true or false returns true only when the input is only digit
#name = name.isalpha()
name = name.replace("a","m")
print(name)

print(help(str))